@use 'variables';
@use 'mixings';

.articleSection {
	margin: 120px auto;
	width: 60%;
	padding: 1em 0;
	.categoryTitle {
		@include mixings.title;
		margin: 0.5em 0 0 0;
	}
	.categorySubtitle {
		@include mixings.subtitle;
		margin: 0.5em 0;
	}
	.articleInfo {
		@include mixings.info;
		margin: 0;
	}
	img {
		margin: 2em 0;
		width: 100%;
		height: 50vh;
	}
	.articleContent {
		font-size: 1.3em;
		text-align: justify;
	}
}
@media screen and (max-width: 675px) {
	.articleSection {
		width: 80%;
		.categoryTitle {
			text-align: center;
		}
	}
}
